# Аудит `configs/entity_scope.csv` от 2026-06-14

Цель аудита: перед новым парсингом привести scope к точным юрлицам группы Сбер/экосистемы, чтобы новые строки добавлялись в core только по доказуемому ИНН и роли заказчика/организатора. Это соответствует протоколу из `docs/collaboration_prompts.md`: сначала качество и воспроизводимость, потом рост объема.

## Итоговое состояние scope

- Файл: `configs/entity_scope.csv`.
- Строк после аудита: `26`.
- Пустых ИНН: `0`.
- Дублирующихся ИНН: `0`.
- `data/reports/quality_summary.json` пока отражает предыдущий прогон (`entities_total = 24`); curated-таблицы и quality summary остаются несинхронными до следующего согласованного source sprint / pipeline refresh.

Update 2026-06-14: после первичного аудита `configs/entity_scope.csv` расширен до identity-card схемы с `entity_key`, `ogrn`, `kpp_list`, `official_name`, `short_name`, `brand_aliases`, `search_terms`, `identity_source`, `identity_confidence`. Правила поиска/принятия описаны в `docs/entity_resolution_protocol.md`. Полный `run-all` не использовать как способ нового парсинга без явного согласия Никиты; источники разбирать отдельными source sprint.

## Что исправлено

### Убрано или объединено

- `ООО Сбербанк-Технологии.Нойдорф` исключен из scope: точный ИНН `7819315503` есть в ЕИС/AST-словарях, но по открытым реестровым зеркалам организация ликвидирована 2014-04-01, а AST exact lookup за 2024-2025 дал `0` процедур. Для периода исследования это исторический хвост, а не рабочее юрлицо.
- `ООО СберМобайл` не ведется отдельной строкой: закупочный контур идет через `ООО Сбербанк-Телеком` (`ИНН 7736264044`), поэтому бренд оставлен в названии `ООО Сбербанк-Телеком (СберМобайл)`.
- Бренды `Купер` / `СберМаркет` заменены на точные юрлица `ООО Инстамарт Сервис` и `ООО Инстамарт Технолоджис`.
- Бренд `СберЗдоровье` заменен на точные юрлица: `ООО Инновационная медицина`, `ООО ДокДок`, `ООО ДокДок ТЗ`.
- Старые brand-only строки `ООО СберБизнесСофт` и `ООО СберРешения` не возвращались в scope: в текущем проходе нет надежного exact-INN/AST сигнала для core-добавления.

### Добавлено или уточнено

- `ООО Сбербанк-Сервис`, ИНН `7736663049`: найден в ЕИС chooser как точное юрлицо; AST exact lookup за 2024-2025 видит `126` процедур до последующих фильтров (`7736663049/775050001:13`, `7736663049/771501001:113`).
- `ООО Сбербанк Инвестиции`, ИНН `5032229441`: найден в ЕИС chooser как точное юрлицо; AST exact lookup за 2024-2025 видит `2` процедуры до фильтров.
- `ООО Облачные технологии (Cloud.ru)`, ИНН `7736279160`: официальный сайт Cloud.ru указывает это юрлицо и ИНН; AST exact lookup видит `498` процедур до фильтров.
- `АО Управляющая компания Первая`, ИНН `7710183778`: добавлено вместо брендового `Сбербанк Управление Активами`; AST exact lookup видит `67` процедур до фильтров.
- `АО НПФ Сбербанка`, ИНН `7725352740`: AST exact lookup видит `5` процедур до фильтров.
- `ООО ДокДок (СберЗдоровье)`, ИНН `9710011437`: AST exact lookup видит `27` процедур до фильтров.
- `ООО СберКорус`, ИНН `7801392271`: официальный сайт СберКорус указывает это юрлицо и ИНН; AST exact lookup видит `6` процедур до фильтров.
- `ООО СК Сбербанк Страхование`, ИНН `7706810747`: Банк России подтверждает юрлицо; AST exact lookup видит `1` процедуру до фильтров.

Важно: эти AST-числа не равны будущему приросту core. Пайплайн еще отфильтрует реализацию имущества, банкротные/asset-sale процедуры, даты, дубли и нерелевантные роли.

## Основные публичные источники для сверки юрлиц

- Kuper contacts: `https://kuper.ru/sp/contacts` - `ООО "Инстамарт Сервис"`, ИНН `9705118142`.
- Kuper business/legal pages: `https://business.kuper.ru/sp/edi` - группа Kuper и юрлица `Инстамарт`.
- Cloud.ru contacts: `https://cloud.ru/contacts` - `ООО "Облачные технологии"`, ИНН `7736279160`.
- Домклик legal info: `https://domclick.ru/terms` - `ООО "Домклик"`, ИНН `7736249247`.
- СберЗдоровье: `https://sberhealth.ru/` и `https://sberhealth.ru/documents/offer.html` - `ООО "Инновационная медицина"`, `ООО "ДокДок"`, `ООО "ДокДок ТЗ"`.
- СберНПФ: `https://sbernpf.ru/about/information-to-be-disclosed/` - `АО "НПФ Сбербанка"`, ИНН `7725352740`.
- Банк России, страховая компания: `https://www.cbr.ru/finorg/foinfo/?ogrn=1147746683479` - `ООО СК "Сбербанк страхование"`, ИНН `7706810747`.
- Сбербанк Страхование Жизни: `https://sberbank-insurance.ru/` - `ООО СК "Сбербанк страхование жизни"`, ИНН `7744002123` встречается в юридических блоках страниц продукта.
- СберМобайл: `https://sbermobile.ru/about/` - `ООО "Сбербанк-Телеком"`, ИНН `7736264044`.
- СберКорус: `https://sberkorus.ru/` - `ООО "СберКорус"`, ИНН `7801392271`.
- УК "Первая": `https://first-am.ru/disclosure/about-company` и раскрываемые PDF-документы - `АО УК "Первая"`, ИНН `7710183778`.

## Локальные evidence-файлы

- AST exact totals: `output/entity_scope_audit/ast_scope_candidate_totals.csv`.
- AST exact totals JSON: `output/entity_scope_audit/ast_scope_candidate_totals.json`.
- ЕИС chooser broad evidence: `data/raw/eis/prompt2/пао-сбербанк-россии_fz223_chooser.html`.
- EIS Prompt 2 source sprint report: `output/source_sprints/eis_prompt2/eis_prompt2_report.md`.

## Рекомендация

Следующий шаг - не массовый `run-all`, а отдельные source sprint по одному источнику:

```powershell
$env:PYTHONPATH='src'
python scripts\eis_prompt2_batch_sprint.py --batch-name <batch> --inn <inn>
```

После каждого source sprint нужно заново проверить:

- `data/reports/quality_summary.json`;
- `data/curated/entity_coverage.csv`;
- прирост/дубли по `procurement_lots.csv`;
- распределение новых строк по `entity_name` и `source_system`;
- README, notebook и отчеты, где еще могут оставаться старые brand-only строки.
